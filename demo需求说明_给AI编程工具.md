# Demo 需求说明书（可直接粘贴给 Deepseek / Codex 实现）

> 用法：把下面「【给 AI 编程工具的完整指令】」整段复制给 Deepseek / Codex，让它按此实现。
> 前面的「背景与设计意图」是给你自己理解和面试讲解用的，可不给工具。

---

## 一、背景与设计意图（面试时这么讲）

这个 demo 是为「中葡经贸中心」（服务中国企业出海葡语/西语 29 国的国家级平台）量身设计的**差异化作品**，用来证明你不是"通用 AI 工程师"，而是"**跨境经贸 AI 工程师**"。

它的设计直击该机构两大第一性痛点：

1. **多语言**：出海企业用中文提问，但目标国政策/法规原文是葡语/西语 → 需要跨语言检索与问答；
2. **可信合规**：政企客户对"可审计"要求极高 → 答案必须**带溯源、给置信度、无依据就拒答**，绝不编造。

面试话术一句话：**"合规问答的及格线不是答对率，是可审计。"**

---

## 二、【给 AI 编程工具的完整指令】

请帮我用 Python 实现一个可本地运行的 **「葡西语国家出海合规智能问答 Demo」**，具体要求如下。

### 1. 项目目标

构建一个面向"中国企业出海葡语/西语国家"的**检索增强生成（RAG）问答系统**。用户可用**中文或葡萄牙语**提问，系统基于内置的国别经贸/合规知识库，返回**带来源溯源、带置信度**的答案；当知识库没有可靠依据时，**明确拒答而非编造**。

### 2. 核心功能（必须实现）

1. **知识库构建**：读取一个文件夹里的若干文档（PDF/Markdown/TXT），完成解析、清洗、切分、向量化并入库。支持增量添加文档。
2. **跨语言 RAG 问答**：中文或葡语提问，都能检索到对应知识（即使原文是另一种语言），并生成答案。
3. **溯源**：每个答案附 1–3 条引用片段 + 来源文件名/章节，界面上可查看。
4. **置信度 + 拒答**：给每个答案一个置信度（高/中/低）；检索相关性低于阈值时，回复"根据现有资料无法可靠回答，建议咨询专业机构"，**不得编造**。
5. **简洁 Web 界面**：一个输入框、语言切换（中文/葡萄牙语）、答案区、来源列表、置信度标识。

### 3. 技术栈（请优先使用）

- 编排框架：**LangChain 或 LangGraph**
- 后端：**Python + FastAPI**
- 向量库：**FAISS**（本地即可，便于跑通；可留 Milvus 接口）
- Embedding：**BGE-M3**（多语言，支持中/葡/西；若下载不便可换 OpenAI/通义 embedding）
- LLM：优先调用**兼容 OpenAI 接口的 API**（如 DeepSeek / 通义 / GPT），把 base_url、api_key、model 做成环境变量可配置；保留"本地 vLLM 部署 Qwen"的可选分支
- 文档解析：pypdf / **LlamaParse**（PDF），Markdown/TXT 直接读
- 前端：**Streamlit**（快速出界面）
- （可选）可观测：LangSmith / Langfuse 接口预留

### 4. 数据源（先用这些公开语料做演示）

- 中国商务部《对外投资合作国别（地区）指南》——巴西、葡萄牙两册（公开 PDF）；
- 可再放 1–2 份目标国投资/税务/用工的公开介绍（中或葡文）。
把这几份放进 `./data` 文件夹作为初始知识库（覆盖≥2 个国别、≥50 个文档片段）。

### 5. 验收标准（实现后逐条自测）

- [ ] 中文提问和葡语提问都能得到合理答案（各举 3 个例子）
- [ ] 每个答案都带来源溯源（文件名 + 引用片段）
- [ ] Top-3 检索命中率 ≥ 85%（用自建 20 条问答对测）
- [ ] 端到端响应 < 3 秒
- [ ] 对一个知识库外的问题，能正确拒答且不编造
- [ ] 一键启动（提供 README 和启动命令）

### 6. 代码与交付要求

- 结构清晰、模块化：`ingest.py`（建库）、`rag.py`（检索+生成）、`app.py`（界面）、`config.py`（配置）；
- README 写明：架构图（文字/流程图即可）、技术选型理由、如何新增文档、如何切换模型；
- 附一个 `eval.md`：列出你自测的问答对和命中率。

### 7. 加分项（时间够再做）

- 巴西葡语 vs 欧洲葡语的术语差异在 README 里说明处理思路；
- 混合检索（关键词 BM25 + 向量）提升命中率；
- 建一个 20–30 条的双语评测集做回归测试。

---

## 三、做完后怎么用

- 把代码放到 GitHub，README 截图放简历/作品集；
- 投递邮件里附 GitHub 链接；面试时可现场演示或录屏；
- 演示时重点讲：**多语言、可溯源、拒答机制、可审计**——这四个词就是你的差异化标签。